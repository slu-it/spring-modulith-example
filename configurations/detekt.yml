build:
  maxIssues: 0

config:
  validation: true
  warningsAsErrors: true

console-reports:
  active: true

## rule configuration ##

naming:
  MatchingDeclarationName:
    active: false

formatting:
  Filename:
    active: false # disabled in favor of 'naming' rule 'MatchingDeclarationName'
